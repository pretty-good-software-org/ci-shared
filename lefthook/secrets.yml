pre-commit:
  commands:
    gitleaks:
      run: gitleaks protect --staged --no-banner
      fail_text: "gitleaks found hardcoded secrets"

pre-push:
  commands:
    gitleaks-full:
      run: gitleaks detect --no-banner
      fail_text: "gitleaks found hardcoded secrets"
