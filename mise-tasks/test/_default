#!/usr/bin/env bash
#MISE description="Run all tests"
set -euo pipefail

mise run test:mdformat-node-modules
node --experimental-strip-types --test actions/*/*/tests/*.test.ts actions/guard/tests/*.test.ts
