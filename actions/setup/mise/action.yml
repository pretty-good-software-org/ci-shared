name: 'Setup Mise'
description: 'Checkout repository and install tools via mise'
inputs:
  mise-env:
    description: 'MISE_ENV value (e.g., "ci")'
    required: false
    default: ''
  github-token:
    description: 'GitHub token for authenticated API requests during tool install (avoids rate limits)'
    required: false
    default: ${{ github.token }}
  fetch-depth:
    description: 'Checkout fetch depth; set 0 for full history (needed by commit-range checks like cog check)'
    required: false
    default: '1'
runs:
  using: 'composite'
  steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: ${{ inputs.fetch-depth }}
    - uses: jdx/mise-action@v4
      env:
        MISE_ENV: ${{ inputs.mise-env || env.MISE_ENV }}
      with:
        # TEMPORARY PIN — mise 2026.7.2 (2026-07-07) hard-fails locked mode on
        # lockfile entries without URLs, which most org mise.lock files have.
        # Remove after fleet lockfiles are regenerated with URL-complete entries.
        version: 2026.7.1
        install_args: '--verbose'
        github_token: ${{ inputs.github-token }}
