name: Template Guard

on:
  workflow_call:

env:
  MISE_ENV: ci

jobs:
  guard:
    name: Template Guard
    runs-on: [self-hosted, Linux, ARM64]
    timeout-minutes: 5
    steps:
      - name: Check out caller repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Run template guard
        uses: pretty-good-software-org/ci-shared/actions/guard@d5b476182db31b83864c22c440f2ee504592dd7b
