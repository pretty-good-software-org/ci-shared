name: Template Guard

on:
  workflow_call:

env:
  MISE_ENV: ci

jobs:
  guard:
    name: Template Guard
    runs-on: [self-hosted, Linux, ARM64]
    timeout-minutes: 5
    steps:
      - name: Check out caller repository
        uses: actions/checkout@v4

      # PIN: replace @main with this PR's immutable squash-merge SHA before callers migrate.
      - name: Run template guard
        uses: pretty-good-software-org/ci-shared/actions/guard@7aa4137fea1b580904d5a6ef20ae6a741c2bb0e1
