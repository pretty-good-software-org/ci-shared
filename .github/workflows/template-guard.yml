name: Template Guard

on:
  workflow_call:

env:
  MISE_ENV: ci

jobs:
  guard:
    name: Template Guard
    runs-on: [self-hosted, Linux, ARM64]
    timeout-minutes: 5
    steps:
      - name: Check out caller repository
        uses: actions/checkout@v4

      # PIN: replace @main with this PR's immutable squash-merge SHA before callers migrate.
      - name: Run template guard
        uses: pretty-good-software-org/ci-shared/actions/guard@0b328e7f5b8da9d55fa3211e5a4225f2db777148
