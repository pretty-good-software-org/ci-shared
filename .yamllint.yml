# Ruleset sourced from the vendored, checksum-pinned organization standard —
# see .lint/configs/yamllint.yml and .org-lint-config.json.
extends: .lint/configs/yamllint.yml
ignore:
  - node_modules/
  - pnpm-lock.yaml
